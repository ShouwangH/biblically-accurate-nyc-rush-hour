/**
 * Root application component.
 * Will be wrapped with providers (DataProvider, SimulationTimeProvider) in future PRs.
 */
export function App() {
  return (
    <div>
      <h1>NYC Rush Hour</h1>
      <p>Scaffold ready. Run tests with `npm test`.</p>
    </div>
  );
}
